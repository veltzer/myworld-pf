# rsconstruct build configuration for this repository.

[build]
# src_dirs entries name directories; "." (the whole tree) is a config error
reject_dot_src_dirs = true

[analyzer.python]

[processor.ruff]
src_dirs = ["scripts"]


[processor.mypy]
src_dirs = ["scripts"]

[processor.shellcheck]
src_dirs = ["scripts", "src"]

# ─── toml / yaml / json ──────────────────────────────────────────────────────

[processor.taplo]
src_files = [".rumdl.toml", "pyproject.toml", "rsconstruct.toml"]

# ─── repo-specific configuration ────────────────────────────────────────────

# The application is a maven war: pom.xml is its dependency manifest (there
# is no other place for the PrimeFaces/JSF dependency closure) and maven does
# the compile and packaging. rsconstruct owns the build and calls maven for
# it, the same way the cargo processor drives cargo from Cargo.toml.
[processor.explicit.war]
command = "scripts/mvn_package.py"
inputs = ["pom.xml"]
input_globs = ["src/**/*"]
output_files = ["target/myworld-pf-1.0.war"]

[dependencies]
system = ["maven"]

# lint the GitHub Actions workflow files with actionlint
[processor.actionlint]
src_dirs = [".github/workflows"]

# ─── markdown ────────────────────────────────────────────────────────────────

[processor.rumdl]
src_files = ["README.md"]

# ─── xml / svg ───────────────────────────────────────────────────────────────

[processor.xmllint]
src_dirs = ["src/main/webapp/WEB-INF"]
src_files = ["pom.xml"]

# ─── java ────────────────────────────────────────────────────────────────────

[processor.checkstyle]
src_dirs = ["src/main/java/com/meta/beans"]
args = ["-c", "checkstyle.xml"]
